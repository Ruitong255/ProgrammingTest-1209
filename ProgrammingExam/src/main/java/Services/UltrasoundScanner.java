package Services;
import SourceCode.MedicalNotification;
import SourceCode.HospitalInformation;


public class UltrasoundScanner extends ServicePanel {

    public UltrasoundScanner(){
        super(service, timeTaken);
        if (m.getHourCount > 3 ){
            h. requestPorter(bay, service);
        }
    }

}
